
<div class="container">
  <h2>Produit recherché :</h2>
    <div class="product-card d-flex">
      <%= image_tag "#{@product.image_url}", class: "product-img" %>
      <div class="product-details">
        <h3><%= @product.title %></h3>
        <p><%= @product.brand %></p>
      </div>
    </div>

  <h2>Consignes de tri :</h2>
    <div class="sorting-card">
    </div>

  <h2>Options :</h2>
    <div class="add-favorites ml-3">
      <% @product.searches.each do |search| %>
        <div class="favorite-btn">

          <%= render 'searches/yellow_star', search: search %>

        </div>
      <% end %>
    </div>
    <div class="add-map ml-3">
      <p><i class="fas fa-map-pin"></i> Où jeter ma bouteille en verre</p>
    </div>
</div>


<!-- ajout du code pour afficher la photo, post cloudinary install -->

<%#= cl_image_tag @product.photo.key, height: 300, width: 400, crop: :fill %>

<!-- ou bien -->

<!-- <img src="" alt="" href="<%= cl_image_path @product.photo.key, height: 300, width: 400, crop: :fill %>"> -->
